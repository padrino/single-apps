# If you want use Padrino from your git checkout
%w(core gen helpers).each do |gem|
  gem 'padrino-' + gem, :path => '/src/padrino-framework/padrino-' + gem
end

# If you want you can use stable gem
# gem 'padrino'

# If you want to use it from git
# gem 'padrino', :git => "git://github.com/padrino/padrino-framework.git"

# Other stuff here
gem 'haml'